# ============================================================
#  Nginx WebSocket Support Map
#  À placer dans : /etc/nginx/conf.d/websocket-map.conf
#  Permet à Nginx de proxifier les connexions WebSocket
# ============================================================

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
